name: Maven Deploy

on:
  push:
    branches:
      - master
      - Issue-2134-ossrh-to-central-portal-migration

jobs:
  maven-deploy:
    runs-on: ubuntu-latest

    # Do not disturb forks
    if: github.repository == 'xspec/xspec'

    steps:
      - uses: actions/checkout@v4

      - run: gpg --version

      - name: Set up Maven
        uses: stCarolas/setup-maven@v5
        with:
          maven-version: 3.9.9

      - uses: actions/setup-java@v4
        with:
          java-version: 8
          distribution: temurin
          server-id: xspec-io.ossrh
          server-username: NEXUS_USERNAME
          server-password: NEXUS_PASSWORD

      - name: Check Maven and Java versions
        run: mvn -v

      - run: mvn -X -Dorg.slf4j.simpleLogger.log.org.apache.http.headers=debug -Dorg.slf4j.simpleLogger.log.org.apache.maven.wagon.providers.http.httpclient=debug clean deploy --batch-mode --activate-profiles release -Dgpg.signer=bc
        env:
          NEXUS_USERNAME: ${{ secrets.maven_username }}
          NEXUS_PASSWORD: ${{ secrets.maven_password }}
          MAVEN_GPG_KEY: ${{ secrets.gpg_private_key }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.gpg_passphrase }}
