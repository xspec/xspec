<?xml version="1.0" encoding="UTF-8"?>
<?xspec-test message=ERROR in x:helper: File at ../../../tutorial/helper/document-nodes/test-helper-documents.xsl is not XProc. It should have /p:declare-step or /p:library. Instead, it has /xsl:stylesheet.?>
<x:description xmlns:x="http://www.jenitennison.com/xslt/xspec"
    xproc="../cases/library.xpl">
    <!-- The .xsl file exists but it's not an XProc file -->
    <!-- When the test runs with the x:run-xproc step, this is a run-time error.
        When the test runs with Ant or a command script, it's a compile-time error. -->
    <x:helper xproc="../../../tutorial/helper/document-nodes/test-helper-documents.xsl"/>
</x:description>
